import 'package:flutter/material.dart';

class NewRecommendationPage extends StatelessWidget {
  const NewRecommendationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('New Recommendation page'),
    );
  }
}
